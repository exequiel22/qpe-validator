using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Test.NetFramework
{
    [TestClass]
    public class TestRules
    {
        [TestMethod]
        public void TestRequiredRule()
        {
            //QPE.Validator.Validator v = new QPE.Validator.Validator();
            //v.AddRule(QPE.Validator.Rules.Required("Mi campo"));
            //object variable = null;
            //Assert.IsFalse(v.Validate(v["Required"], variable));
            //variable = "ALGO";
            //Assert.IsTrue(v.Validate(v["Required"], variable));
            //variable = "     ";
            //Assert.IsFalse(v.Validate(v["Required"], variable));
            




          
        }
    }
}
